version: '3.3'
services:
#  motioneye:
#    image: ccrisan/motioneye:master-amd64
#    ports:
#      - "8765:8765"
#    volumes:
#      - ./motioneye/config:/etc/motioneye
#      - ./motioneye/data:/var/lib/motioneye
#    restart: unless-stopped

  ip-camera-viewer:
    build:
      context: ./ip-camera-viewer  # Указываем папку, где лежит Dockerfile
      dockerfile: Dockerfile  # Имя Dockerfile
    ports:
      - "5001:5000"
    volumes:
      - ./ip-camera-viewer:/app  # Монтируем папку app в контейнер
    environment:
#      - RTSP_URL=rtsp://<логин>:<пароль>@<ip_камеры>:554/stream1
      - RTSP_URL=rtsp://admin:admin123@192.168.1.103:554/stream1
    restart: unless-stopped